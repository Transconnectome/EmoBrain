# Archived Target-Supervised Diagnostics

These scripts are preserved only to reproduce pre-2026-07-22 diagnostic
results. They are not part of the canonical EmoBrain model, encoder comparison,
or run sequence.

The supported encoder families are E1 ViT and E2 BFM. New experiments must use
the configs under `project/code/configs/`.
